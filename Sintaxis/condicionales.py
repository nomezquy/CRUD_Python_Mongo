# condicional if
# if condicion:
    #bloque a ejecutar tabulado
# else:
    #bloque a ejecutar tabulado.

#Condicional anidada

"""
if condicion:
    #bloque a ejecutar tabulado
if condicion:
    #bloque a ejecutar tabulado

En el caso anterior se validan todos los IF.



if condicion:
    #bloque a ejecutar tabulado
elif condicion:
    #bloque a ejecutar tabulado.

    En el anterior caso si uno de los IF o elif es valido ya no se validan los demas.
"""